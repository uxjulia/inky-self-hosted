# Public URL for the browser app.
FRONTEND_PORT=3000

# Frontend deployment mode shown in the Help page.
# Use hosted for a future public URL build, or self-hosted for Docker/local use.
VITE_INKY_APP_MODE=self-hosted

# API storage. SQLite lives inside this named volume by default.
INKY_DATABASE_URL=sqlite:////data/inky.db
INKY_DATA_DIR=/data

# Optional host folder exposed as read-only books in Local Library.
INKY_LOCAL_LIBRARY_PATH=./library

# Optional folder scanned by the API during local development outside Docker.
INKY_MOUNTED_LIBRARY_DIR=./library
